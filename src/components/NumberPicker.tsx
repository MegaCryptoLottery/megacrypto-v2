import { useEffect, useState } from 'react';
import { GlobalPrizeDashboard } from './GlobalPrizeDashboard';
export const normalizeTicketNumbers=(numbers:number[])=>{const normalized=[...new Set(numbers)].sort((a,b)=>a-b);if(normalized.length!==15||normalized.some(n=>!Number.isInteger(n)||n<1||n>25))throw new Error('A ticket requires exactly 15 unique numbers from 1 to 25.');return normalized;};
function useSound(){const [enabled,setEnabled]=useState(()=>localStorage.getItem('megacrypto_sound')!=='off');useEffect(()=>{localStorage.setItem('megacrypto_sound',enabled?'on':'off');},[enabled]);const tone=(frequency:number,duration=.06)=>{if(!enabled)return;try{const ctx=new AudioContext(),osc=ctx.createOscillator(),gain=ctx.createGain();osc.frequency.value=frequency;gain.gain.setValueAtTime(.04,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+duration);osc.connect(gain).connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+duration);}catch{}};return{enabled,setEnabled,tone};}
export function NumberPicker({value,onChange}:{value:number[];onChange:(numbers:number[])=>void}){const{enabled,setEnabled,tone}=useSound();const toggle=(number:number)=>{if(value.includes(number)){onChange(value.filter(n=>n!==number));tone(280);}else if(value.length<15){const next=[...value,number];onChange(next);tone(next.length===15?740:510);}else tone(150);};const lucky=()=>{const pool=Array.from({length:25},(_,i)=>i+1),next:number[]=[];while(next.length<15)next.push(pool.splice(Math.floor(Math.random()*pool.length),1)[0]);onChange(next.sort((a,b)=>a-b));tone(660,.12);};return <><GlobalPrizeDashboard/><section className="panel picker" aria-labelledby="ticket-title"><div className="section-heading"><div><p className="eyebrow">YOUR TICKET</p><h2 id="ticket-title">Choose 15 numbers</h2></div><div className="picker-tools"><span className={value.length===15?'ready-count':'muted'}>{value.length} / 15 selected</span><button className="icon-button" aria-label={enabled?'Mute sounds':'Enable sounds'} onClick={()=>setEnabled(!enabled)}>{enabled?'🔊':'🔇'}</button></div></div><div className="number-grid">{Array.from({length:25},(_,i)=>i+1).map(n=><button key={n} className={value.includes(n)?'picked':''} aria-pressed={value.includes(n)} onClick={()=>toggle(n)}>{String(n).padStart(2,'0')}</button>)}</div><div className="picker-actions"><button className="secondary" onClick={()=>{onChange([]);tone(220);}} disabled={!value.length}>Clear</button><button className="secondary" onClick={lucky}>Lucky pick</button><span>{value.length===15?'Your ticket is ready for review.':'Choose 15 unique numbers to continue.'}</span></div></section></>;}

