import { CHAINS } from '../config/chains';

export function Fairness() { return <section className="panel fairness"><p className="eyebrow">FAIRNESS & RANDOMNESS</p><h2>Per-network verifiability</h2><p>“Integration verified” means verified explorer source imports <code>VRFConsumerBaseV2Plus</code> and deployment constructor arguments identify a coordinator. It does not prove every draw used VRF: request ID <code>0</code> is the contract’s documented manual-contingency path.</p><div className="fairness-grid">{Object.values(CHAINS).map((chain) => <article key={chain.key}><b>{chain.name}</b><strong className={chain.contracts.vrf.status}>{chain.contracts.vrf.status === 'integration-verified' ? 'Integration verified' : 'Verification pending'}</strong>{chain.contracts.vrf.coordinator && <span>Coordinator: {chain.contracts.vrf.coordinator}</span>}<small>{chain.contracts.vrf.note}</small></article>)}</div><p className="data-limitation">Draw events are correlated only by their actual request ID. V2 does not infer a Chainlink request from array order or event timing.</p><a href="https://docs.chain.link/vrf" target="_blank" rel="noreferrer">Learn how Chainlink VRF works ↗</a></section>; }
