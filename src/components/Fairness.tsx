export function Fairness() { return <section className="panel fairness"><p className="eyebrow">FAIRNESS & RANDOMNESS</p><h2>Randomness verification is pending.</h2><p>The deployed lottery ABI exposes a real <code>SorteioRealizado(requestId, maskSorteada)</code> event, but the verified registry does not include coordinator, subscription, callback, or request-transaction configuration proving a Chainlink VRF integration for each deployment. V2 therefore does not label any network “VRF verified”.</p><p className="data-limitation">Draw history is withheld until an audited deployment block range is configured; no query starts at block 0.</p><a href="https://docs.chain.link/vrf" target="_blank" rel="noreferrer">Learn how Chainlink VRF works ↗</a></section>; }
