import { CHAINS } from '../config/chains';
export function NetworkGrid({ selected, onSelect }: { selected: string; onSelect: (key: keyof typeof CHAINS) => void }) { return <section className="panel"><div className="section-heading"><div><p className="eyebrow">MULTI-CHAIN</p><h2>Choose your network</h2></div><span className="muted">All deployments require verification</span></div><div className="networks">{Object.values(CHAINS).map(chain => <button key={chain.key} className={`network ${selected === chain.key ? 'selected' : ''}`} onClick={() => onSelect(chain.key)}><span className="orb"/><span>{chain.name}</span><small>{chain.contracts.status === 'verified' ? 'Live' : 'Awaiting verification'}</small></button>)}</div></section>; }
